# # Bathymetry Inversion via Frequency Decomposition + Dual Models

> **目标**  
> 基于重力异常与重力梯度数据，反演海底地形（Bathymetry），  
> 通过 **频域分解 + 模型分工** 的方式分别建模全局低频与局部高频结构，  
> 最终在空间域进行物理一致的重构。

---

## 一、总体思想（绝对不要改）

**核心公式：**

h(x, y) = h_low(x, y) + h_high(x, y)

其中：
- h_low：> 65 km 的长波（全局、平滑）
- h_high：< 65 km 的短波（局部、起伏）

👉 **不是一个模型学全部，而是两个模型各司其职**

---

## 二、频域分解（只做一次，离线）

### 2.1 输入数据
- GEBCO 原始水深（规则网格）

### 2.2 操作流程
1. 对 GEBCO 做 2D FFT
2. 使用 **65 km 高斯低通滤波器**
3. 得到：
   - 低频谱 → IFFT → h_low
   - 高频谱 → IFFT → h_high  
     （或 h_high = h - h_low）

### 2.3 重要原则（禁止违反）
- ❌ **不在网络里做 FFT**
- ❌ **不让网络自己学“频率划分”**
- ✅ 频率分解必须是**确定性的物理操作**

---

## 三、模型 1：MLP（低频 / 全局）

### 3.1 建模目标
学习 **平滑、连续、全局变化的地形背景**

### 3.2 输入 / 输出
- 输入（点）：
  - 重力异常
  - 重力梯度
  - 经度
  - 纬度
- 输出：
  - h_low（该点的低频水深）

### 3.3 模型形式
- MLP / Residual MLP
- 本质：**隐式地形函数 f(x, y)**

### 3.4 为什么不用 CNN？
- 低频信息不依赖局部感受野
- 经度 / 纬度破坏 CNN 的平移等变性
- CNN 学低频 = 浪费参数 + 不稳定

---

## 四、模型 2：CNN（高频 / 局部）

### 4.1 建模目标
学习 **局部起伏、短波、地形残差**

### 4.2 Patch 思想（核心）

CNN **不能按点训练**，必须看到邻域：

- 将整个区域划分为 **重叠局部 patch**
- 每个 patch 独立训练 / 推理
- 推理后再拼接

### 4.3 输入 / 输出

#### 输入（patch）
- 重力异常 patch
- 重力梯度 patch  
（可选：低频 h_low patch）

> ❗ **不输入经纬度**
> CNN 必须保持平移等变性

#### 输出（patch）
- h_high patch（局部高频水深）

---

## 五、CNN 的 Patch 设计原则（防止踩坑）

### 5.1 Patch 大小
- 典型：32×32 / 64×64
- 必须覆盖 **短于 65 km 的空间尺度**

### 5.2 Stride
- 小于 patch size（例如 1/2）
- 保证推理阶段可以 **重叠平均**

### 5.3 为什么 patch 是对的？
- 高频信息是 **局部相关**
- CNN 的感受野 ≈ 物理短波范围
- Patch = 人为定义的“局部物理窗口”

---

## 六、训练阶段（严格分离）

### 6.1 MLP 训练
- Dataset：点数据
- 标签：h_low
- Loss：MSE

### 6.2 CNN 训练
- Dataset：patch 数据
- 标签：h_high
- Loss：
  - MSE
  - + 梯度损失（可选，用于锐化地形）

> ❗ **两个模型完全独立训练**
> ❌ 不共享 loss
> ❌ 不联合端到端

---

## 七、推理阶段（最重要的一步）

### 7.1 推理流程

1. MLP 推理：
   - 输入：grav, curv, lon, lat
   - 输出：h_low_pred（全图）

2. CNN 推理：
   - 滑窗遍历所有 patch
   - 得到 h_high_pred（全图，重叠区域取平均）

3. 最终结果：
   
   h_pred = h_low_pred + h_high_pred

---

## 八、这个框架的“底层逻辑”

### 8.1 本质是什么？
这是一个：

> **频域约束的多模型反演系统**

而不是“一个大网络硬拟合”。

---

## 九、绝对不要做的事（黑名单）

- ❌ 让 CNN 输入经纬度
- ❌ CNN 按点训练
- ❌ 用 UNet 同时学低频 + 高频
- ❌ 让网络自己学频率分离
- ❌ 端到端强行联合训练

---

## 十、当你怀疑“是不是走歪了”的自检问题

如果你在某一步犹豫，问自己：

1. 这个改动 **会不会破坏频率分工？**
2. 这个模型 **真的需要空间感受野吗？**
3. 这个输入 **是否符合模型的归纳偏置？**

只要有一个回答是“不确定”，先停。

---

## 十一、总结一句话

> **低频靠位置 + 物理约束，高频靠局部结构。**  
> **先分频，再建模，最后相加。**

