### TODO: 物理正则（谱约束 / 平滑）
### loss += physics_loss(pred, X)
### TODO: 用 gMLP block 替换 self.blocks
### TODO: 在 input 前加入 Fourier Features
### TODO: 改成 multi-branch 输入（grav / curv 分支）

swot的分辨率没有gebco高，gebco降采样？还是swot插值
傅里叶变换后2个高斯滤波是否合适，2和65km的带宽是否合适，

目前没有下载NCEI作为真值

研究区选什么比较合适
怎么划分训练和测试样本比较合适，按区域还是按点？
按点是否会受邻近点影响，按区域是否会学不到普遍特征？

重力异常和重力梯度处理不知道对不对，感觉问题很大

高斯滤波之后是否需要有向下延拓这个步骤呢？
向下延拓的目的是什么，是否为必须操作？？


损失函数的设计，看怎么设计会比较好一点，不要是单纯的统计量，换成物理公式损失看看？？

目前gebco只使用了nc，还没有使用tid

tid是什么东西？？

按点训练和按块训练效果完全不一样，按点训练，输入经纬度的话会固化模型，降低泛化能力，

重力异常和重力梯度转了频率后再转回来感觉很受水深影响，z0带来很大的相关性导致泛化性不高，应该是重力数据和重力梯度不做处理，
转水深数据来分成长波短波来训练。

或者后续可以对比一下转了频率的和原始的有没有什么差异

mlp->预测长波，topo低频
cnn->预测短波，GGM高频
过程逻辑同学院论文，相加也同学院论文得了

或者
mlp->gebco低频
cnn->gebco高频
相加得总深度
## 陈信学姐的论文思路：

### NCEI数据处理：
#### 1.以GEBCO2023为参考，计算深度差异，去除差值超过标准差3倍，相对误差大于0.5的点
### MGGM：
#### 1. topo_25.1用高斯滤波平滑得到参考深度dref
#### 2. 用参考深度dref计算短波重力异常分量 
- ![短波重力异常计算公式](markdown_pircture/chen_xing_3.png)
- G=6.672 × 10−8 cm3 g−1 
- Δ𝜌是海水和岩石密度差异
- D是参考模型最大深度
#### 3. 用参考深度dref去计算长波重力异常分量
- ![长波重力异常计算公式](markdown_pircture/chen_xing_4.png)
#### 4. 从SUDST2022GRA模型中移除应用topo_25.1计算的长波分量得到短波分量𝑔𝑆𝐷𝑈𝑆𝑇2022𝐺𝑅𝐴𝑠ℎ𝑜𝑟𝑡
- ![另一个重力数据的短波重力异常计算公式](markdown_pircture/chen_xing_5.png)
#### 5. 公式三逆运算计算短波分量dref-D，加上dref得到深度结果
- ![另一个重力数据的短波重力异常计算公式](markdown_pircture/chen_xing_6.png)
- Δ𝜌
  = 8.7 cm3 g−1（迭代计算和船测深度相关性自强得到）

### BPNN
#### 1. 输入数据（应该是按网络不是按点训练）
##### 1.1 重力异常模型
- SDUST2022GRA模型
- EGM2008模型（扩展至球谐度2160）
##### 1.2 海底深度模型
- 制作的MGGM_BAT(就是上面那个流程制作得到的)
- 高斯滤波（windowsize=10km）后的topo_25.1
- 未高斯平滑的topo_25.1
##### 1.3 经纬度
##### 1.4 损失函数：MSE，未添加物理约束制作特别的损失函数


## 目前毕设思路：
### 1. 数据预处理
#### 1.1 重力数据
- 重力异常，重力梯度暂不处理，
#### 1.2 海底深度模型
- 对海底深度模型进行类似GGM方法分割，傅里叶变换后，用高斯低通滤波分割为低频、高频2部分，分别转回空间域得到dlong和dshort，然后分别用不同的模型训练
### 2. 网络选择，结构
#### 2.1 dlong的学习模型（MLP）
- dlong选择使用适合提取全局特征，全局上能保存连续性的网络进行训练，目前暂时使用有残差连接的MLP来学习，损失函数暂时使用MSE，以点来训练。
- 后续看效果，损失函数加上物理约束
#### 2.2 dshort的学习模型（UNet）
- dshort选择适合提取局部特征的模型，Unet上下采样可以学习到局部特征间的联系，区块训练，损失函数加了一点物理约束（地形梯度一致性）
- 后续看效果，不好的话用其他cnn网络尝试
#### 2.3 后续改进方向（想法）
- 将mlp和unet合并起来训练，损失函数用各自的损失加权，权重搞一个sigmod函数，随高度变化，具体的还没想好
### 3. 输入变量
- 重力异常，重力梯度，经纬度作为输入特征，gebco模型的海底深度作为标签
### 4. 验证精度
- 拿去和有NCEI的点的深度去对比，计算预处的误差，gebco的误差，看一下效果
### 

## 实验中遇到的问题
### 1. mlp直接预测过于平滑，但是预测长波分量又不够平滑
### 2. Unet直接预测短波分量，仅输入重力相关数据会发生震荡，学习不到相关物理约束，会出现多解问题，预测效果特别烂
